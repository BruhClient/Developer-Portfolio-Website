import { WorldCanvas } from "@/world/react/WorldCanvas";

/*
  Temporary home for the walkable world while it is built. The real homepage is
  still the scrolling site; `/` only becomes the world in milestone 4, once
  there is content in here worth arriving at.
*/
export default function WorldPage() {
  return <WorldCanvas />;
}
